require("codemirror/mode/php/php");
require("codemirror/keymap/vim");

window.Split = require("split-grid").default;
console.log(Split);

window.CodeMirror = require("codemirror");
window.hotkeys = require("hotkeys-js").default;
